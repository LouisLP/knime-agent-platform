# Release checklist — v0.1.0

- [x] `.env.example` documents every variable, with no real credentials
- [x] Backend fails at boot on invalid config instead of on the first request
- [x] MCP session opens once at boot and closes on SIGINT/SIGTERM
- [ ] Tool-running indicator in the chat UI
- [ ] Screenshot in the top-level README
- [ ] Time-allocation breakdown written up
